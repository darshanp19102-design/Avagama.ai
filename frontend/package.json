{
  "name": "avagama-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "node -e \"const fs = require('fs'); fs.writeFileSync('env.js', 'window.__APP_ENV__ = ' + JSON.stringify({ API_URL: process.env.API_URL || '' }) + ';'); console.log('Generated env.js');\""
  }
}